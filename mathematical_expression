// Program to sove a mathematical expression.
#include <iostream>
int main() {
    float x,y;
    std::cout<<"Enter value of x: ";
    std::cin>>x;
    std::cout<<"Enter value of y: ";
    std::cin>>y;
    std::cout<<(x+10)/(3*y);
    return 0;
}
